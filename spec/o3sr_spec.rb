# frozen_string_literal: true

RSpec.describe O3sr do
  it "has a version number" do
    expect(O3sr::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
